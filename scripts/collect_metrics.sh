# Collects system metrics such as CPU usage, memory usage, GPU details, disk status, and temperature.
# Logs data to both the database (metrics.db) and JSON (historical_data.json).
# Uses commands like lscpu, sensors, and df.
